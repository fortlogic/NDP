# Top Level
SRC = src
BUILD = build/${ARCH}_${PLATFORM}

# Clash/GHC Settings
CLASH_OUT = ${BUILD}/clash
CLASH_ODIR = ${CLASH_OUT}/o
CLASH_HIDIR = ${CLASH_OUT}/hi
CLASH_INCLUDES = ${SRC}

CLASH_ENTITIES = ${SRC}/NDP/Entity
CLASH_VHDL = ${SRC}/vhdl

# GHDL Settings
GHDL_OUT = ${BUILD}/ghdl

# ROMs
TILE_MAP = ${SRC}/Resources/ROM/Tiles/TileMap.hs

# Xilinx Settings
XILINX_OUT = ${BUILD}/xilinx
VM_ROOT = /vagrant
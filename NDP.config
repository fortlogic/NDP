# Build Paths
BUILD_DIR = build
CLASH_OUT = ${BUILD_DIR}/clash-${CLASH_VER}

# Sources
CLASH_SRC = src/clash

# Clash/GHC Settings
CLASH_ODIR = ${CLASH_OUT}
CLASH_HIDIR = ${CLASH_OUT}
CLASH_HDLDIR = ${CLASH_OUT}
CLASH_INCLUDES = ${CLASH_SRC}

# ROMs
TILE_MAP = src/rsrc/tile-rom/TileMap.hs